name: Test ARC Runner

on:
  workflow_dispatch:

jobs:
  test-arc-runner:
    runs-on: zombienet-arc-runner 
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: test
        run: echo "hello world"
